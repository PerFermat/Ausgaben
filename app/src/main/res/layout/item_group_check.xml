<?xml version="1.0" encoding="utf-8"?>
<!-- Eine Gruppe im Zuordnungsdialog: Kontrollkästchen, dahinter der Name. Die Zeile für eine neue
     Gruppe in dialog_account_groups.xml ist genauso gebaut, nur mit einem Eingabefeld statt des
     Textes – so stehen die Namen aller Zeilen an derselben Stelle. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="48dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/rowCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="0dp"
        android:paddingEnd="0dp" />

    <TextView
        android:id="@+id/rowText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceBodyLarge" />
</LinearLayout>
